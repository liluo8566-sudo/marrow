# Today — 2026-05-28 Thu

## Principle
- Keep going until the goal is truly achieved.
- If live (user-like) verification is possible, run it before reporting.
- The only standard of goal verification is whether it works in practice. Tests and dry runs are just safeguards.

## Dispatch Policy (read first)
- Strictly follow `.claude/rules/agent-dispatch.md`.
- You are the orchestrator — dispatch to agents / worktrees and keep context clean.
- Decide agent count and type yourself. Prefer Sonnet for coding; Haiku for scan/lookup; reserve Opus.
- No "Lumi pending" in any output. If unsure, brainstorm + first-principle yourself; ask only when it's a real product-level question only Lumi can answer.

## Today

### Session 1 (main, running) — Python watcher crash root cause + fix
**/goal pgrep -f "marrow.watcher" returns one PID alive for ≥30 min AND the newest `~/Library/Logs/DiagnosticReports/Python-*.ips` has mtime older than that PID's start time**

- Outcome: watcher stops crashing; 24h crash count drops from 6 to 0.
- Status: fix applied; Monitor armed 1900s window until 15:28:30. Wait for signal.

### Session 2 (main + 1 worktree) — atlas 4 bug + cc-lab → CC-Lab + external/ (drift_sweep real-world test)
**/goal pytest -q exit 0 AND pgrep -f "marrow.watcher" PID alive AND `grep -rIn "/CC-Lab/" ~/CC-Lab ~/.claude ~/Library/LaunchAgents ~/Toolkit ~/.config/marrow 2>/dev/null` returns only archive/log paths (no live code/config refs) AND renaming `~/.claude/skills/grill` → `grillme` test produces a drift alert in marrow alerts table**

- Outcome: atlas 4 leftover bugs cleared; cc-lab repo renamed to CC-Lab with external/ subdir holding all third-party repos; every drift ref auto-tracked; bug4 watchdog dir-rename works.

Tasks (in order):
1. **atlas bug 1** — `marrow/atlas.py::atlas_sweep_fs` retract walks ancestor chain; any AUTHORIZED_ROOT ancestor with depth=0 retracts all stub-only descendants regardless of intermediate depth.
2. **atlas bug 2** — one-time `DELETE FROM atlas WHERE path` outside AUTHORIZED_ROOTS; add guard in sweep + reconcile.
3. **atlas bug 3** — introduce `ATLAS_ROOT_ORDER` constant decoupled from `AUTHORIZED_ROOTS` iteration order. Order: Study → NY → CC-Lab → .claude → .config → Toolkit. Wire into `build_atlas_spec.section_order`.
4. **atlas bug 4** — extend `marrow/watcher.py` DriftHandler to handle watchdog `on_moved(is_directory=True)`. Queue rename events for each child path so `drift_sweep.handle_move` scans refs. Apply existing `EXCLUDE_DIRS_*` filtering.
5. **cc-lab → CC-Lab + external/** — drift_sweep first real-world batch:
   - `mkdir ~/CC-Lab/external/`
   - `mv ~/CC-Lab/{claude-buddy,weclaude,image-gen-mcp} ~/CC-Lab/external/`
   - `mv ~/cc-lab ~/CC-Lab`
   - drift_sweep auto-tracks: python/shell/plist hardcoded paths · `~/.claude/settings.json` additionalDirectories + trustedDirectories · `~/.claude/CLAUDE.md` @import refs · `marrow/drift_sweep.py` AUTHORIZED_ROOTS · `rules/files.md` + DESIGN + FUTURE + DECISIONS + handover refs · project `.claude/` configs · worktree paths
   - drift_sweep reports missed refs or errors → fix on the spot, this is its intended stress test
   - AUTHORIZED_ROOTS adds `~/CC-Lab/external/` for ongoing watch

Out of scope (held for another window):
- **atlas PreToolUse hook** — Lumi designs in a separate window; do NOT implement here.
- **atlas hint backfill** (44 rows × note/write/naming) — wait until hook design lands, then propose draft for Lumi review.

Dispatch:
- worktree `wt-atlas-bugs` (general-purpose, Sonnet) — implements tasks 1-4 + pytest green; main reviews diff, merges, live-verifies; main session executes task 5 directly (root rename touches main repo + cross-system refs, can't isolate in worktree).
- main session live-verify: `launchctl kickstart` watcher; rename `~/.claude/skills/grill` → `grillme`, drift alert fires; `grep -rIn "/CC-Lab/"` clean.

### Session 3 (main + Explore agent) — NY memm retire
**/goal `ls ~/Library/LaunchAgents/com.ny.*.plist 2>/dev/null` empty AND `ls ~/Toolkit/scripts/ny-memm-*.py ~/Toolkit/scripts/ny_lib.py ~/Toolkit/scripts/ny-skip-now.py 2>/dev/null` empty AND `ls ~/.claude/hooks/ny-session-*.sh ~/.claude/hooks/ny-session-start.sh.bak.* 2>/dev/null` empty AND `[ -d ~/.config/ny ]` false AND `[ -d ~/.claude/skills/skip-memm ]` false AND `grep -c "legacy ny-memm" ~/.claude/rules/files.md` == 0 AND `ls ~/CC-Lab/archive/ny-memm/scripts/` non-empty**

- Outcome: NY memm backend system fully offline; Toolkit scripts archived to CC-Lab/archive/ny-memm/; hooks + skip-memm skill + logs gone. NY base folder content (memory/ + code/) Lumi handles separately.

Tasks:
1. `rm ~/Library/LaunchAgents/com.ny.{curator.weekly,rotate.daily,cleanup.monthly,compress.monthly,retire.monthly}.plist`
2. `mkdir -p ~/CC-Lab/archive/ny-memm/scripts/` (path uses post-rename root from S2)
3. `mv ~/Toolkit/scripts/{ny-memm-*.py,ny_lib.py,ny-skip-now.py} ~/CC-Lab/archive/ny-memm/scripts/`
4. `rm ~/.claude/hooks/ny-session-{start,end}.sh ~/.claude/hooks/ny-session-start.sh.bak.*`
5. `rm -rf ~/.config/ny/`
6. `rm ~/Library/Logs/ny-{curator,rotate,compress}.{out,err}.log ~/Library/Logs/ny-hook-timing.log ~/Library/Logs/ny-memm.log`
7. Edit `~/.claude/rules/files.md` — delete the "legacy ny-memm = `ny-`" clause.
8. `rm -rf ~/.claude/skills/skip-memm/`

Dispatch:
- main session executes directly — all `rm` / `mv` / one-line edit, no agent needed.
- Explore agent only if mid-task something unfamiliar surfaces (unknown reference, unexpected file).

Out of scope:
- `~/Desktop/NY/code/` + `~/Desktop/NY/memory/` — Lumi handles manually.
- `settings.json` `/Desktop/NY` in trustedDirectories — keep (Garden / .obsidian still need it).
- `mw-goose-bites.plist` not loaded + `com.nianyu.move-downloads` not loaded — flag in handover for Lumi review, not today's task.

### Session 4 — Phase 4 WeChat / cyberboss direction
Lumi runs this as a separate brainstorming session. Not in today's executable plan.

## Constraints
- Concurrent agents ≤ 3.
- atlas PreToolUse hook design — separate Lumi-led window; this plan does not implement it.
- atlas hint backfill — hold until hook design lands.
- mm+, study inserter, pit disk-SoT — shipped 05-27 evening, not on today's list.
- Hold: cheatsheet (FUTURE), placement_rules.toml.

## Followup (to handover at session end)
- atlas PreToolUse hook design — pending Lumi brainstorm.
- atlas hint backfill — pending hook design.
- atlas root section order Study → NY → CC-Lab → .claude → .config → Toolkit landed in S2.
- `mw-goose-bites.plist` + `com.nianyu.move-downloads` both not-loaded — Lumi review whether to load or remove.
- cyberboss brainstorm — Lumi separate window.
