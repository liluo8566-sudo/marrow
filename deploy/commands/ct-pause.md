---
description: Cortex — DND on.
---

Run `~/CC-Lab/cortex/.venv/bin/python -m cortex.ctl pause` via Bash and report its one-line output. Exit DND later with `/ct-wake` (wakes AND unpauses) or `python -m cortex.ctl resume` (unpauses without waking).
